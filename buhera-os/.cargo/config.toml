# buhera-os-specific cargo config.
# Keeps the build directory inside this workspace so binaries land at
# buhera-os/target/{debug,release}/ rather than the shared repo-root
# target.

[build]
target-dir = "target"
